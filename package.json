{
	"name": "morphorm-workspace",
	"private": true,
	"author": {
		"name": "Jean Caiza",
		"email": "jeanmcm@hotmail.com",
		"url": "https://github.com/jfortez"
	},
	"repository": {
		"type": "git ",
		"url": "git+https://github.com/jfortez/morphorm.git"
	},
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"build": "turbo run build",
		"dev": "turbo run dev",
		"lint": "turbo run lint",
		"lint:fix": "turbo run lint:fix",
		"fmt": "turbo run fmt",
		"fmt:check": "turbo run fmt:check",
		"build:pkg": "turbo run build --filter=@morphorm/core --filter=morphorm",
		"test": "turbo run test",
		"check-types": "turbo run check-types",
		"changeset": "changeset",
		"ci:version": "changeset version && bun update",
		"ci:publish": "for dir in packages/*; do (cd \"$dir\" && bun publish || true); done && changeset tag",
		"version-packages": "changeset version",
		"release": "bun run build && changeset publish",
		"release:core": "bun run build:core && changeset publish --filter=@morphorm/core",
		"release:morphorm": "bun run build:morphorm && changeset publish --filter=morphorm",
		"release:all": "changeset publish",
		"build:core": "turbo run build --filter=@morphorm/core",
		"build:morphorm": "turbo run build --filter=morphorm",
		"prepare": "husky"
	},
	"dependencies": {
		"zod": "^4.3.6"
	},
	"devDependencies": {
		"@changesets/changelog-github": "^0.5.2",
		"@changesets/cli": "^2.30.0",
		"@happy-dom/global-registrator": "^20.8.4",
		"@testing-library/jest-dom": "^6.9.1",
		"@testing-library/react": "^16.3.2",
		"husky": "^9.1.7",
		"lint-staged": "^16.4.0",
		"oxfmt": "^0.35.0",
		"oxlint": "^1.55.0",
		"turbo": "^2.8.17",
		"typescript": "5.9.2"
	},
	"lint-staged": {
		"*.{js,ts,mjs,cjs,jsx,tsx}": [
			"oxlint",
			"oxfmt --no-error-on-unmatched-pattern"
		]
	},
	"engines": {
		"node": ">=18"
	},
	"packageManager": "bun@1.3.8"
}
